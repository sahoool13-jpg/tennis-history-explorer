# Phase 3 — Web

A DuckDB-wasm frontend that loads the built Parquet artifact from `data/built/`
and lets you explore tennis history entirely in the browser — no backend.

**Status:** stub — not yet implemented.
