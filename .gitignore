# Raw upstream data — fetched locally via fetch_data.sh, never committed
data/raw/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Notebooks
notebooks/.ipynb_checkpoints/

# OS / editor cruft
.DS_Store

# Web app (Phase 3)
web/node_modules/
web/dist/
# Parquet/palette are copied from data/built at dev/build time — single source
# of truth stays in data/built, never duplicated into the app.
web/public/data/
